# Example Digit Images

This folder contains small 28x28 PNG digit examples copied from the old development environment. They are provided only as lightweight sample inputs for the prediction command.

Example:

```bash
python scripts/train_dense_mnist.py --mode predict --model-path models/mnist_dense.keras --image-path data/examples/Digit1.png
```
